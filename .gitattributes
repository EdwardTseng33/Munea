# 臉機補丁與部署小工具必須保持 LF——Windows 簽出被 autocrlf 轉成 CRLF 會讓 Linux GPU 機 git apply 失敗
# （2026-07-24 test_flashhead_patch_integrity 在新 worktree 亮紅燈後立）
*.patch -text
*.sh text eol=lf

# Localized legal pages are transformed and compared byte-for-byte in CI.
# Keep both the source and Firebase hosting copies on LF across Windows/macOS/Linux.
web/legal/** text eol=lf
web/src/legal.css text eol=lf
app-site/legal/** text eol=lf

# 2026-07-29：Windows 上用程式重寫檔案會偷偷把 LF 換成 CRLF，
# 一次 80 行的改動在紀錄上長成上萬行全檔重寫、每行的來歷全被蓋掉（PR #297 踩過）。
# 這裡釘死行尾，工具再怎麼寫都不會再髒進來。
*.py text eol=lf
*.json text eol=lf
*.mjs text eol=lf
*.js text eol=lf
