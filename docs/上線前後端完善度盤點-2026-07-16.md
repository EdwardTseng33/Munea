# 上線前後端完善度盤點 · 2026-07-16

> 由 Windows 蘇菲三路並查產出（後端程式面／維運面／上線閘門文件），2026-07-16 18:00。
> 用途：後端架構完善度項目的底帳。每消一項就更新狀態欄；新 session 接手先讀這份、不必重盤。
> 原則：只列「後端／架構」範疇；App 包版、真人手機測試、Edward 本人要按的不在此份。

## 一、已修（本次盤點直接動工）

| 項 | 內容 | 狀態 |
|---|---|---|
| AI 端點限流護欄 | 12 條燒錢入口（/chat、/voice-note、/memory/* 等）同人同端點每分鐘 60 次上限、超額回 429。 | ✅ PR #115 · STATUS 94 號 |

## 二、程式都寫好、卡在「還沒部署」（最高槓桿＝發一次部署車）

一次 Brain＋Voice 部署可同時上線：

1. **Voice 台語硬閘＋開場收音＋人格記憶調校**——正式流量還跑 7/14 舊版（無語言閘、還帶舊台語指令）；7/14 晚之後所有修正都只在 main 或 0% 金絲雀。
2. **Brain #101 用藥照片伺服器端剝除**——隱私政策一致性，已裝舊版 App 仍會夾帶照片上雲，只有伺服器端擋得住。
3. **Apple 訂閱通知接收端**——正式 Brain `/apple/notifications` 回 404（staging 活著）；Edward 已在 App Store Connect 填好兩條通知網址，等接收端上線才能發 TEST 驗收。
4. **通知中心後端＋東京 017**——設定 API／伺服器端停發已寫好，部署＋東京跑 017 才生效。
5. **本份 PR #115 的限流護欄**——合併後同車生效。

> 卡點：Mac 無部署工具（無 gcloud）。兩條路待 Edward 拍板：Ⓐ 交 Windows 蘇菲跑（工具與登入都在）Ⓑ Mac 裝 Google Cloud SDK＋Edward 登入一次。詳 STATUS 93 號⑦。

## 三、後端程式面缺口（未動工、依風險排序）

| # | 缺口 | 風險 | 說明 |
|---|---|---|---|
| 1 | ~~AI 端點零限流~~ | ~~財務~~ | ✅ 已修（PR #115） |
| 2 | 推播外寄匣沒有定時觸發 | 功能性 P0 | 通知只在有人打 `/admin/notifications/drain` 時才真的送；全 repo 沒有任何排程呼叫它。用藥提醒／危機告警堆在匣裡不會出去。與 APNs 金鑰、Cloud Scheduler 決策綁定。 |
| 3 | 帳號刪除的登入身分清理無重試 | 合規尾巴 | 刪除個資成功但 Auth 身分刪除逾時（4 秒）失敗時，只發一則 Slack、無持久化重試佇列 → 該 email 無法重新註冊、要人工補刀。建議：待清理佇列＋維護排程重掃。 |
| 4 | 資料匯出只有「整包塞在回應裡」 | 低 | 有真匯出（約 30 張表、本人範圍、fail-closed），但無伺服器端產檔／下載連結／email 交付，落檔靠 App 端。App Store 可接受、非擋。 |
| 5 | 請求編號太弱 | 除錯效率 | request id 用毫秒時間戳、併發會撞、成功路徑與 log 都不帶 → 用戶回報的錯誤對不到伺服器紀錄。順手修等級。 |
| 6 | 訂閱方案偏好變更事件（DID_CHANGE_RENEWAL_PREF）沒有專門處理 | 低 | 降級偏好要等下次續扣才生效，回 200 不重試、非致命。 |

## 四、維運面缺口（未動工、依風險排序）

| # | 缺口 | 風險 | 說明 |
|---|---|---|---|
| 1 | Brain／Voice 零監控告警 | 最高 | 兩個核心服務沒有 uptime 檢查、沒有錯誤通報、沒有 log 保留設定；Gateway 監控只顧總機且預設只看不報。**服務靜默掛掉＝等用戶打不通才知道。** 補法便宜：GCP uptime check 打 `/healthz`＋告警接既有 Slack webhook。 |
| 2 | 三類定時任務全未接線 | 高 | ① 晨報 06:30（指令備好未執行；且 `supabase/sql/009` 未套用＝就算掛了也存不進）② 席位過期回收（RPC＋端點在、無人定時呼叫＝容量與計費會漂）③ 推播外寄匣（見程式面 #2）。 |
| 3 | 備份沒演練過還原＋資料庫格式檔台帳漂移 | 資料安全底線 | 東京排程備份有開但從未演練還原、無 RTO/RPO；線上落後 repo：018 待跑（Edward）、017 存疑、009 從沒套用。另本機 `.env.local` 直連正式庫（無獨立開發庫）。 |
| 4 | 後端無自動部署 | 中 | 只有公開網站自動上線；Brain／Voice／Gateway 全手動。部署腳本本身健全（合併語意、金絲雀→驗證→轉正兩步閘）。 |
| 5 | 過時文件未標記 | 低 | 雙環境／Vercel 世界觀的舊文件（STAGING-BACKEND-RUNBOOK、PRODUCTION-INFRA-READINESS-2026-07-02 部分段落等）沒標「已取代」，7/13 排查曾因此誤判。部署 SOP 的資料庫網址段也還寫雪梨。 |

## 五、帳務上線閘門殘項（`docs/BILLING-CREDITS-ENTITLEMENT-v1.md` Launch Gates）

| 項 | 現況 | 卡點 |
|---|---|---|
| Policy v3（013）套用正式庫並驗證 | migration 已定義、未套用 | 東京庫執行（Edward／Mac） |
| 訂閱通知 V2 端點上線＋TEST＋Sandbox 驗收 | 程式 ✅、正式 404 | 部署車（見二-3） |
| 伺服器端每分鐘通話扣點端到端 | 未完成 | 涉 Gateway（Codex 鎖區）、需協調認領 |

## 六、其他備查

- **每日聊天時長上限**（容量護欄）：文件有建議值（90 分軟提示／180 分關注）、程式無實作；差 Edward 拍數字＋一段扣點前檢查。首波單卡容量時最有感。
- **月點數重置／訂閱降級清理**：伺服器端已做（讀取時自動結算、到期通知觸發失效）——不缺排程，先前文件說缺是誤解。
- **家庭圈降級後既有超額成員裁減**：只擋新加入、不裁既有超額圈；邊角案例。
