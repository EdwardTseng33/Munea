# 沐寧 Munea · 健檢修復排程（2026-07-01）

> 來源：城堡三方健檢（架構=卡西法 / 資安=沙利曼 / 規劃=蘇菲，2026-07-01）。
> 用法：**認領前先看這張，動工前去 `協作看板-雙AI分工.md` 登記、完工更新。** 不設硬性分工線；靠登記＋避讓避免重複。
> 認領欄：空=沒人做、🏰=城堡、🤖=Codex。

---

## 🔴 P0 — 收真實用戶／上架前必解

| # | 項目 | 來源 | 主要動到 | 認領 | 狀態 |
|---|---|---|---|---|---|
| 1 | **中文危機詞庫**：Guardian 補中文自傷/急症/醫療/情緒詞（現全英文、中文長輩喊救命偵測不到） | 卡 P0-1 / 沙 | `engine/model_router.py` | 🏰 | ✅ 完成 |
| 2 | 個資示範檔＋執行期 JSON 移出版控（`.gitignore` + `git rm --cached`），示範資料改假名 | 沙 #3 | `.gitignore`、`engine/*_store.json`、`user_profile.json` | 🤖 | ✅ 完成 |
| 3 | 後端全端點驗身份（credits/entitlements/privacy/chat/memory 目前裸奔） | 沙 #1 / 卡 | `engine/server.py`、`scripts/smoke.ps1`、`scripts/auth-gate-smoke.ps1` | 🤖 | ✅ 後端 auth gate 與正式模式 API smoke 完成；真 Supabase Auth provider E2E 待 live env 驗證 |
| 4 | 點數/訂閱帳目搬 Supabase＋前端不可偽造餘額/權益 | 沙 #1 | `server.py`、`supabase_adapter.py`、`supabase/sql/` | 🤖 | ✅ 後端防偽造/追責底座完成；正式 provider JWS/Webhook 驗證另列後續 |
| 5 | 子女看長輩健康數據：連結後完整顯示（Edward 2026-07-01 定案）| 沙 #2 | `supabase/sql/` | 🏰 | 🔄 決定已定＝健康數據全開；縮小為「連結時長輩一次同意＋原始對話不攤開＋寫入收後端」，跟 Codex 對 schema 後落地（見 [設計文件](RLS-子女授權同意分層-設計與待決-2026-07-01.md)）|
| 6 | 境外語音（Gemini/OpenAI）知情同意 UI＋隱私權政策連結 | 沙 #4 | `web/`（onboarding） | 🤖 | ✅ 前端知情同意 baseline 完成：onboarding 完成前需勾選，設定頁可查看/更新，App 內隱私權政策頁已補；後端同意紀錄待正式帳號資料層接續 |
| 7 | JSON store 原子寫入（temp+rename）防程式中斷資料無聲遺失 | 卡 P0-5 | `server.py` | 🤖 | ✅ 完成 |
| 8 | server 改 threading/async，防單線程被一個慢請求卡死全部 | 卡 P0-4 | `server.py` | 🤖 | ✅ 完成 |

## 🟡 P1

| # | 項目 | 來源 | 認領 | 狀態 |
|---|---|---|---|---|
| 9 | 兩套記憶系統收斂成一套（`chat_engine` 舊中文 profile vs `model_router` SPEC schema） | 卡 P0-3 | 🏰 | 🔄 Claude/城堡主線：`engine/memory_engine.py` 已建，接下來會動 `server.py` / `chat_engine.py` / `supabase/sql`；Codex 暫避同檔，待推後接 smoke/契約補強 |
| 10 | 行為正確性測試：危機/付費（現 smoke 只測契約在不在、不測對不對） | 卡 P1-1 | 🏰 | 🟢 危機測試完成（`engine/test_guardian_crisis.py`·20/20 過）；付費正確性測試待 |
| 11 | 推播回路（主動關心/危機通知家人/吃藥提醒）目前 0%→建 | 卡 P1-2 | — | ⏸️ |
| 12 | `memory_extract` 中文關鍵字（現英文、中文對話幾乎萃不到記憶）＋ `model_router.py:527` 感知健康詞中文化 | 卡 P0-1 附 | 🤖 | ✅ deterministic 中文保底完成：偏好/家人/作息/情緒/健康脈絡可萃取；live Butler/Claude 萃取另列後續 |
| 13 | `except: pass` 吞錯 → 至少 log（debug 期難查） | 卡 P1-4 | 🤖 | ✅ backend / legacy chat engine fallback logging 完成；smoke 已加 AST 護欄避免再出現 silent except/pass |

## 🔵 核心大工程（最難最久）

| # | 項目 | 來源 | 認領 | 狀態 |
|---|---|---|---|---|
| 14 | 反射腦真接 Gemini Live 即時語音迴路（現為 stt-chat-tts fallback） | 卡 P0-2 | 🏰 | 🔄 Claude/城堡主線：Gemini Live / WebSocket / 即時語音體感；Codex 避讓 `engine/live_voice_*` 與即時語音 web 接線 |
| 15 | Avatar 會動的臉（2D viseme → Ditto/LiveAvatar PoC gate 後接） | — | — | ⏸️ |
| 16 | `server.py` 拆檔（84KB/2000 行單體，過警戒線） | 卡 P2-3 | — | ⏸️ |

---

## 建議順序（城堡視角）
1. **本週快修**：#1 中文危機詞庫（完成）→ #2 個資出版控。便宜、擋掉「聽不懂救命」＋「洩漏真人資料」。
2. **收用戶前**：#3 #4 #5 #6 #7 #8。展示屋→真產品的分界鎖。
3. **核心**：#14 即時語音 + #9 記憶收斂 + #15 Avatar。

> 完整證據見三份健檢原報告（架構/資安/規劃）。SPEC §9 商業、TAM 重算屬霍爾 CPO 職權、不在本技術排程內。

---

## 聊聊三模組落地排程（2026-07-02 同步後）

> 來源：`聊聊-分層服務架構-2026-07-02`、`聊聊-架構總覽與目標-2026-07-02`、`記憶層與感知層-強化設計-2026-07-02`。
> 對外三模組：**記憶 / 感知 / 交互**。對內仍維持可換技術層＋指揮層。
> 目前原則：Claude/城堡正在主攻**記憶層強化**，且會動 `server.py` / `chat_engine.py` / `supabase/sql`；Codex 暫避記憶接線同檔，先做排程協調、後續契約驗證、感知/指揮層拆分與非重疊測試。

| 順序 | 代號 | 工作 | 建議協作者 | 主要檔案 | 驗收 |
|---:|---|---|---|---|---|
| 1 | M1 | 記憶單一真相：`memory_engine.py` 接 `/memory/extract` / `/butler/post-turn`，只存使用者事實，不存 AI 回話 | Claude/城堡主線 | `engine/memory_engine.py`、`engine/server.py`、`engine/chat_engine.py`、`supabase/sql/` | smoke 驗「AI 回話不入記憶」與中文長輩事實入 `memory_items` |
| 2 | M2 | 舊 `user_profile.json` 遷移策略：喜好/回憶/興趣權重轉成 `memory_items`，舊檔退為相容/匯入 | Claude/城堡主線 | `engine/chat_engine.py`、`engine/server.py` | local JSON fallback 與 Supabase adapter 以 `memory_items` 為主 |
| 3 | M-QA | 記憶主線推完後的契約補強：Codex 補 smoke、auth/data-rights/export 檢查、回歸測試 | Codex 接手驗證 | `scripts/smoke.ps1`、必要時 docs | 不改記憶主邏輯；只補測試/契約，避免重工 |
| 4 | P1 | 感知最小真源：時間/日期/時區＋今日簡報資料格式；等 Claude 記憶接線推完後再動 `server.py` | Codex | 新 `engine/perception_engine.py`、`engine/server.py`、`scripts/smoke.ps1` | `/perception/snapshot` 可產生 time snapshot；回話 context 能吃到今日狀態 |
| 5 | P2 | 天氣真源：接中央氣象署 CWA API 或可替換 provider adapter，先服務寒流/高溫/散步建議 | Codex | `engine/perception_engine.py`、env 範例、docs | 無 API key 時安全 fallback；有 key 時可存 weather snapshot |
| 6 | I1 | 指揮層顯式化：安全＞即時＞背景管家的優先權與 scoped context 注入規則 | Codex 與 Claude 對齊後做 | `engine/server.py` 或新 `engine/orchestrator.py`、`scripts/smoke.ps1` | Guardian high/critical 可蓋過一般 persona/memory/perception；context 不整包亂塞 |
| 7 | V1 | 即時語音與互動體感：Gemini Live、WebSocket、打斷、輪替節奏、主動開口口感 | Claude/城堡主線 | `engine/live_voice_*`、web voice wiring | Codex 不主動碰；只在需要時補合約/驗證 |
| 8 | B1 | 付費/權益正確性測試：不碰記憶/語音主線，可作為 Codex 等待期間的平行任務 | Codex | `scripts/smoke.ps1`、billing/credits tests | 驗證 credits/entitlements 行為正確，不只合約存在 |
| 9 | A1 | 會動的臉：2D viseme 強化與後續雲端 Avatar API / Ditto PoC gate | 待排 | web avatar runtime、`/avatar-session` | 先驗留存/成本，再決定自架 GPU |
