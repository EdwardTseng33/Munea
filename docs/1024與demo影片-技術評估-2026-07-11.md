# 🎬 1024 高清臉 × Demo 影片 · 技術評估（2026-07-11 · 查規格不試錯）

> Edward 要「演算框放頭→肩、1024 生成、但輸出/顯示完整全身照片」＋一支 20 秒 1024 demo 取代官網影片。
> 這頁只講技術可行性與路徑；成本/毛利數字見 `成本與毛利-現行棧-2026-07-11.md`（財務長）。

## 事實（SoulX-FlashHead 官方規格，已查證）
- **Lite**：單 4090 96FPS（或 3 路並發即時 25FPS）。輸出 ~**512**。← 我們現役
- **Pro**：單 4090 **10.8FPS（不即時）**；真即時要 **2×RTX5090**。品質更高、但 native 仍 ~512（官方寫 high-quality、沒寫 1024）。**目前未下載**（只裝了 Lite）。
- **結論：「1024」不是這家模型的內建解析度**——不管 Lite/Pro，要真 1024 都得**外掛超解析（SR）**把輸出放大＋補細節。

## 三條路 × 適用場景
| 路徑 | 即時聊天可行？ | Demo 影片可行？ | 成本/風險 |
|---|---|---|---|
| Lite 512（現役） | ✅ 3並發 | ✅ | 最低；但糊（512 天花板） |
| Lite + 超解析(512→1024) | 🟡 SR 吃 GPU、要測即時餘裕 | ✅ | GPU 成本上升、幅度待測 |
| Pro 512（高品質） | ❌ 4090 僅 10.8FPS（要 2×5090） | ✅（預錄不怕慢） | 即時要換 5090×2＝GPU 成本數倍 |
| Pro + 超解析 | ❌（更重） | ✅ 最佳畫質 | Demo 首選、即時不划算 |

## 🎯 建議路線
- **Demo 影片（20 秒、預錄、取代官網）**：走 **Pro（或 Lite）+ 超解析 512→1024**，離線慢慢算不怕不即時。要先下載 Pro（~數 GB）＋接超解析＋接 Gemini Live 語音生一段 → 女巫 Gate 2 驗畫質 → 換上官網。**這是一個獨立製作任務、可排。**
- **即時聊天要不要上 1024**：先看財務長的「1024 GPU 成本 vs 499/999 毛利」——若即時 1024 讓毛利流血，launch 先維持 512（或 Lite+輕量 SR 測餘裕），1024 當日後高階檔加值。**別為了即時 1024 硬換 5090×2 燒錢**（v5.4.23 燒錢 Gate）。

## 目標架構（演算框頭→肩、顯示全身）落地備忘
- 演算框放大到頭→肩＝改 CHAR_SRC 裁切框（現 `[0,7.29%,100%,75%]` 臉區 → 放大含肩）＋ overlay 框同步放大；模型輸出該區、貼回全身立繪。**要測：Lite 對「更大區域」的動態品質與 FPS**（區域大＝每格內容多、可能掉速）。
- 顯示端永遠是完整 9:16 全身照（立繪背景 + 動區貼回）——見 `聊聊臉-顯示與影音-定案手冊.md`。

## 待辦（排程、非今晚硬幹）
1. 財務長算完 1024 成本/毛利 → Edward 拍即時要不要 1024
2. Demo 影片：下載 Pro → 接 SR + Gemini Live → 生 20s → 女巫驗 → 換官網
3. 演算框放大到頭肩：先在測試機量「大區域 Lite 的 FPS/品質」再決定
