# 沐寧 Munea · 開發日報 — 2026-07-12（週日）

> 蘇菲整理。Edward 傍晚交代「寫今日總結＋明日待辦＋需要你提供什麼」，並授權「我人不在、你自己判斷完成整個開發」。
> 本日主軸：對標 GPT 健康管家補齊安全能力 → 版本整合 1.28.0 → 多人容量攻堅（語音 30 人達標）→ 清晨備料上線規格 → 情緒補全。全程沒動正在服務的機器、沒跟 Codex 打架。

---

## 一、今日開發總結（已存已上傳）

### A. 安全能力（對標 GPT 健康照護管家設定）
1. **盤點 + 補齊計畫**：`docs/安全能力補齊計畫-對標GPT健康管家-2026-07-12.md`。八題 2 滿分 / 5 半分 / 1 空白；安全底線（不診斷不調藥急症轉介）實測站穩。
2. **守護腦加三類危險辨識**（`engine/model_router.py`）：精神異常/被害妄想（不強化妄想）、保護事件家暴/虐待/財務剝削（轉介 113、不自動通知家人）、傷害他人；補委婉自傷「一走了之」＋藥物問句「藥可以停」漏接。
3. **說明書紅線擴充**（`engine/chat_engine.py`）：不強化妄想/保護事件轉 113/他傷/排他依附 A 案軟界線/情緒勒索親情壓力接法（中立接住不選邊）。
4. **情緒辨識補全**（`model_router.py`，commit e787c7d）：補內疚/羞愧/挫折/失落/混亂，對標 GPT 12 情緒。
5. **守護腦接回語音線·第一層**（卡西法，`live_voice_server.py`）：關鍵字並行監聽字幕、命中即記事件＋告警＋下句轉介、不卡通話（模擬驗過）。
- Edward 拍板：排他依附走 A 案（牽掛保留＋不說排他句）；擬真臉＝AI 合成臉。

### B. 前端 / 合規
6. **首頁招呼亂碼修復**（v1.26.5）：原本直接塞上通電話第一句語音逐字稿→遇雜訊變亂碼；加「乾淨閘」＋顯示端二次過濾。瀏覽器實測過。
7. **AI 生成揭露**（沙利曼 Gate 5）：`onboarding.html`/`terms.html`/`privacy.html` 補「臉與聲音是 AI 即時生成、不是真人」；四條授權規矩入條款。手機畫面量測過排版。

### C. 版本整合
8. **1.28.0 整合**（commit f685811）：把蘇菲的安全＋揭露＋授權，乾淨疊在 Codex 的 1.27.0（多人保護）上，未動繁中鐵律/多人保護/方案 B。selftest 20/0/3、版本三處一致。

### D. 多人容量攻堅（今日最大突破）
9. **語音天花板從 5-10 人 → 30 人乾淨過關**：
   - 去抖修（commit 7e95e77）：簡報背景執行緒堆積止血（111→0），但非斷崖兇手。
   - **async 修（commit d9ab9cb）：真兇＝`live_voice_server.py:456` session 建立時同步阻塞查詢卡住 async 主迴圈；用 `asyncio.to_thread` 移背景 → 10人 40%→100%、15人 0%→100%、30人 0%→100%；首次回聲 p50 N=30 仍 4.2s。**
   - 新天花板＝Gemini API 配額（APIError:1011，N=20 尾端首見）＝可申請/多金鑰分流解決的乾淨問題。
10. **容量 + 成本兩份規劃**：`docs/多人併發容量架構-2026-07-12.md`（卡西法：顯卡池/排隊取代退語音/卡數三檔）＋`docs/多人併發成本與定價-2026-07-12.md`（sophie：定價全不用改、毛利 39-75%、每分鐘 NT$0.54-0.86）。
11. **1-30 人在線架構**：交通指揮台（查顯卡＋語音空位才放行）＋顯卡池自動擴縮＋滿載排隊不退純語音＋即時監控機動調整。

### E. 清晨備料（上線規格）
12. **清晨備料實作**（commit ce457fa）：`perception_engine` 加 `fetch_tomorrow_preview`（隔天預告）＋`fetch_weekly_topics`（當週多元話題、護欄）；`server.py` 整合＋補 `reply_context_instruction` 渲染進 AI prompt；上線規格驗收全過。
13. **🔴 挖到真根因**：`perception_snapshots.snapshot_type` CHECK constraint 從建表就缺 `daily_briefing` 值 → 清晨簡報從上線起就存不進（23514）。修復 `supabase/sql/009_...sql` 已備（純新增值、零風險）、**待執行**。

### 今日 commit（都已 push main）
- f685811 · 1.28.0 整合
- 7e95e77 · 簡報去抖
- d9ab9cb · async 修（打破 30 人斷崖）
- ce457fa · 清晨備料上線規格
- e787c7d · 情緒辨識補全

---

## 二、明日待辦（蘇菲自主推進中，不需等）

1. **守護腦第二層**（拐彎危機 AI 判語意）：軟訊號清單（model_router 純規則）＋語意判讀（Gemini Flash）＋接進語音線 guardian_watch。分批做、每步測。← **今晚接續開發中**
2. **情緒/守護腦測試固化**進 `test_guardian_crisis.py`（上線規格·回歸完整）。
3. **排隊機制**（拔掉 app.js 退純語音 → 排隊）：上線前必備、產品定義層級。⚠ 動 app.js（Codex 密集地盤）需協調。
4. **基本監控**（同時人數/卡使用率/排隊深度）：上線前必備。
5. **心理諮商框架**（CBT/ACT/MI）寫進說明書：安全能力批次 1。
6. **語音線第二層 + 真機安全測**：守護腦語音線目前只模擬測、未接真 Gemini Live 真人測。

---

## 三、需要 Edward 提供 / 授權（解鎖球在你手上）

### 🔴 對上線有直接幫助
1. **貼 009 SQL**（Supabase → SQL Editor 貼 `supabase/sql/009_perception_snapshot_daily_briefing.sql` 內容執行）→ 解鎖清晨備料能存＋根治老簡報毛病（30 秒、零風險）。
2. **補 `MUNEA_ADMIN_API_TOKEN`** 到雲端服務（7/9 舊缺口 R6）→ 後台功能完整。
3. **Google 用量額度申請**（Generative Language API，配額頁）→ 語音超過 30 人才需要（首波限量內不急）。
4. **Mac 重打包上 TestFlight** → 你手機才吃得到今晚全部修復（首頁/語音 30 人/揭露/安全）。

### 🟡 優化加分（可緩）
5. **開測試顯卡**（NT$50 內，你終端機跑 `podctl.py create` 或明說允許蘇菲開卡）→ 免費畫質升 768 ＋一卡三人驗證。
6. **GLOWS 查詢通行碼**（放 `deploy/glows/.env`）→ 釐清現行畫質 512 還 640。
7. **建 Cloud Scheduler**（每天台灣 06:30 打 `/admin/daily-briefing`）→ 清晨備料自動跑（要先做第 1 項）。
8. **美國備用機開回**（RunPod）→ 補「只有台灣一台會斷」的備援。

### 需你拍板
9. **定價 + 每日聊天時長上限**（sophie 建議現價不改、上限＝容量保護非怕虧）。
10. **首波容量策略**：省錢優先（有人才開卡、偶爾等）vs 體驗優先（保溫秒接）——蘇菲＋sophie 建議省錢優先。

---

*日報 v1 · 2026-07-12 深夜 · 蘇菲。Edward 授權不在時自主完成開發，本報告為狀態快照，後續進展續記白板 STATUS.md。*
