# AI 成本 · 真帳單在哪＋自家記帳計畫（2026-07-22）

> 起因：Edward 問「成本怎麼一直只列顯卡的、語音腦跟大腦都沒列」。
> 答案：大腦的錢記在 Google 的帳單上，我們自己的系統**從來沒有幫它記帳**。本檔＝真帳位置的唯一對照表＋補記帳的施工計畫。
> 舊成本文件（`成本與毛利-現行棧-2026-07-11.md` 等）裡的 Gemini 數字全是「官方單價×假設」的**估算**，不是帳單。

## 一、錢分兩本帳（現況）

| 成本 | 記在哪 | 看得到嗎 |
|---|---|---|
| 顯卡（動臉 GPU） | RunPod 後台（儲值制） | ✅ 一眼看到餘額——所以成本文件只有它有實錘 |
| 大腦＋語音腦＋查資料＋記憶索引（全是 Gemini） | Google 帳單 · Munea 專案 `gen-lang-client-0229303523` → 帳戶 `01F8C7-2F30B5-73224E`（My Billing Account） | ⚠️ 要開 [Google 帳單報表](https://console.cloud.google.com/billing/01F8C7-2F30B5-73224E/reports?project=gen-lang-client-0229303523) 才看得到；這本帳可能同時掛著其他專案，看時要按專案篩「Munea」 |

- 已確認：Munea 專案掛在付費帳戶上（billingEnabled: true）＝**不是免費額度**，用多少算多少。
- 三把 Gemini 鑰匙（Munea-key1/2/3）都在同一個 Munea 專案 → 正式機＋測試機的 AI 花費**全部匯進同一本 Google 帳**，不會漏記、只是我們沒去看。

## 二、過去 30 天真實用量（2026-06-22 → 07-22 · 從 Google 監測數據拉的，非估算）

| 項目 | 次數 |
|---|---|
| 全部 AI 請求 | 約 2,164 次 |
| 文字回話（generate_content） | 916 |
| 記憶語意索引（embedding 批次） | 345 |
| 查資料（search grounding） | 333 |
| 其餘（語音通話連線等） | ~570 |

- 量級判讀：**等於只有 Edward 在測試**。照官方單價換算，一個月大約落在幾十到幾百元台幣；精確金額以帳單頁為準。
- 語音通話（Live）分鐘數目前**拉不到**——Google 監測只給次數不給 token/分鐘，這正是要自家記帳的原因。

## 三、缺口與計畫（Edward 2026-07-22 點頭「更新完善」）

**缺口**：伺服器程式（Brain／Voice）完全沒有用量記帳——沒在數每通電話、每次對話用了多少 token，營運後台也沒有 AI 成本卡（只看得到顯卡）。上線後語音是成本大宗、還有「碎聊多 3 成」的逐輪累積計費地雷（見 2026-07-11 成本文件），沒儀表＝失血看不見。

**施工計畫（AI 成本記帳器）**：
1. **記帳**：Brain／Voice 每次呼叫 Gemini 後，把回傳的用量數字（token 數、通話累計）寫進東京庫新表 `ai_usage_log`（誰、哪條功能、哪個模型、多少量、時間）。
2. **換算**：後端一個端點把用量×官方單價換算成台幣日/月報。
3. **呈現**：營運後台加一張「AI 成本」卡，跟顯卡成本並排——Edward 一頁看全。
4. **保險**：Google 帳單設預算警報（例：月 US$50 通知），防悄悄失血。
5. 上線後用真帳單回填校準 7/11 成本模型的「碎聊 +35%」假設。

狀態：已排進待辦（BACKLOG）；動工時照包版雙向回報規矩登記（純後端＋後台、不影響 App 包版，但 Voice/Brain 要跟部署車一起上線才生效）。

---

## 四、2026-07-30 00:3X 重大更正（燒到見底那晚查證後補 · 上面第二節的用量數字已過期）

> ⚠️ **本節優先於前面所有數字。** 7/22 那句「等於只有 Edward 在測試」已不成立。

### 4-1 錢其實有**三本**帳，不是兩本（7/22 漏掉的）

| 成本 | 記在哪 | 有沒有事前警告 |
|---|---|---|
| 顯卡（動臉 GPU） | RunPod 後台（儲值制） | ✅ 看得到餘額 |
| Gemini（大腦／語音腦／查資料／記憶索引） | Google 帳單 · 帳戶 `01F8C7-2F30B5-73224E` | ✅ 預算警報「沐寧月度費用警戒」TWD 1000、50/90/100% 三段 |
| **⭐ AI Studio 預付額度（真正付 Gemini 的那個錢包）** | AI Studio → 專案帳單 | 🔴 **沒有任何事前警告** |

**2026-07-29 深夜事故就是第三本燒光**：正式大腦與聊聊兩台同時啞掉、真的用戶打不通，
**而八盞服務巡邏燈全綠**——因為巡邏只問「機器有沒有回應」、不問「她還講不講得出話」。
Google 那個預算警報看不到這個錢包，所以完全沒叫。

**現行接法（PR #341）**：`engine/ai_health.py` ＋ 巡邏 `check: "ai-alive"`——搭真流量便車
（零成本）記成功／失敗，沒人用時才自己探一次（快取 20 分鐘）。判定鐵律：**「不知道」不等於「沒事」**。
這是**事後偵測**，事前警告目前仍然沒有。

### 4-2 真實用量（7/29 單日 20 小時 vs 7/22 記的 30 天）

| | 請求數 |
|---|---|
| 7/22 文件：整整前 30 天 | 2,164 |
| **7/29 單日 20 小時** | **5,111** |

**主因是開發驗證、不是用戶**——當天跑了五輪 30 題真模型考卷
（每輪 ≈ 30 題 ×（3 輪對話 ×（生成＋鐵律評審）＋7 維評審））。
**教訓：開發燒的錢要跟營運分開看，而且要有自己的預算。跑整份考卷前先想清楚要跑幾輪。**

### 4-3 單位成本重算（1 點 ＝ 1 分鐘通話）

| 項目 | 每分鐘 |
|---|---|
| 動臉顯卡（NT$15.7/hr ÷ 1.6 併發） | NT$0.17 |
| 語音模型（Live 聽＋說） | NT$0.33–0.6 |
| **說明書每輪重算重計費** ⭐ | 明顯高於 7/08 模型估的 US$0.002 |
| **合計** | **NT$0.8–1.1** |

- Plus **599／100 點**（現行；7/08 文件寫的 499/200 點已失效）→ 蘋果抽 15% 後實拿 **NT$509**
- 100 分鐘全部用完 → 成本 **NT$80–110 ＝ 實拿的 16–22%、毛利 78–84%**
- **結論：599 夠，而且很夠——因為它賣的是 100 分鐘、不是吃到飽。**
  超量再買點數（售 1.67–2.5／點 vs 成本 0.8–1.1）一樣賺。

**⚠ 可優化項**：常駐說明書**每一輪都重送、重計費**。2026-07-29 一天之內從 4,920 token
長到 6,481（+32%，加了中醫／看診／天氣三段規矩），這一項現在跟語音本身差不多貴。
**方向：不常用的規矩改成「用到才載」，不要整份常駐。**

### 4-4 三個要停止再犯的誤解（Edward 7/30 親自糾正）

1. ❌「打字聊天不扣點是個沒天花板的洞」→ **Munea 根本沒有打字聊天產品**。
   `/chat` 是**通話**在用的大腦接口（語音辨識轉文字送過去），App 裡沒有輸入框。
   **使用者唯一的消耗路徑就是通話，而通話有計量表。**
2. ❌「顯卡保溫是 24 小時固定成本、一台整月 NT$11,300」→ **管家閒置 900 秒自動收卡**。
   `munea-runpod-controller`（Cloud Run）方針＝用完刪、要用開全新、排隊 ≥1 人就開卡，
   Glows 快照優先 → RunPod 印象檔備援。詳 `docs/4090三人併發-兩軌計畫-2026-07-23.md`。
3. ❌「該去設預算警報」→ **早就有了**（見 4-1）。真正的缺口是它看不到 AI Studio 那個錢包。

> **這三個都是我讀了舊文件就直接下結論造成的。**
> 寫成本或風險結論前，先查現況、再引文件——**文件會過期，跑著的東西不會。**
